define(['angular'], function (angular) {
  'use strict';

  return angular.module('game.controllers.villageCtrl',[])
    .controller('VillageCtrl', ['$scope','socket', 'gameProfile',
      function ($scope, socket, gameProfile) {      

    }])
})